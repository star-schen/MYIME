# 项目协作约定

- 默认所有测试由用户负责。除非用户明确授权，不运行测试、编译、语法检查、模拟器/设备/界面验证、流程演练或冒烟测试，也不以改名规避。可以阅读和修改代码；交付如实说明未测试。用户在当前任务中的明确测试授权优先。
- 有实质影响且上下文无法确定的需求、冲突或决策，先问用户；期间只继续不依赖答案的工作。
- 已授权的改动完成后，应提交并同步到 `https://github.com/star-schen/MYIME`。保留远端历史，不 force push；不提交 `.deps`、工具链、构建产物、运行时用户词库或凭据。推送受阻时明确报告，不假称已同步。
- 搜索代码优先 `rg` / `rg --files`。按业务职责组织代码，保持高内聚、低耦合，避免重复维护与过度拆分。
- Windows Host 只负责平台适配；输入算法由官方 librime/schema/dictionary 实现。按键路径不得使用 IPC。
- `include/myime/core.h` 是 Host/Core ABI 契约，修改时同步 Rust FFI；使用 opaque handle，明确线程、字符串和释放约束。
- 产品配置与原生 Rime YAML 分离，保留用户 patch；不得按 EXE 名猜测词库或游戏兼容策略。
- 不在 DllMain 做 COM/Rime 初始化、部署、文件 I/O 或启动工作线程。不要将未测试的游戏兼容性或桌面端到端输入描述为已通过。
